package com.dsa.slidingWindow.variableWindow;


/*
 * Problem: Largest Subarray With Given Sum
 *
 * Given an array of integers and an integer K, find the length of the longest
 * contiguous subarray whose sum is exactly K. (Optionally, you may also return the
 * start and end indices of such a subarray.)
 *
 * Example:
 * arr = [4, 1, 1, 1, 2, 3, 5], K = 5
 *
 * Valid subarrays with sum = 5:
 * - [4, 1]          -> length 2
 * - [1, 1, 1, 2]    -> length 4   (longest)
 * - [2, 3]          -> length 2
 * - [5]             -> length 1
 *
 * Output:
 * Longest length = 4  (subarray [1, 1, 1, 2])
 *
 * Note:
 * - If the array contains only non-negative numbers, this can be solved with a
 *   variable-size sliding window in O(n).
 * - If negatives are allowed, typically use prefix-sum + hashmap in O(n).
 */
public class Largest_SubArray_With_Given_Sum_2 {
	public static void main(String[] args) {

	}
}
