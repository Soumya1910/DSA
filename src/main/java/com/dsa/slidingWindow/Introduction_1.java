package com.dsa.slidingWindow;

/*
* How to identify if this problem statement is Sliding Window problem?
*
* Use Sliding Window when you need results over a CONTIGUOUS subarray/substring (a “window”).

* Key Identifiers:
* => Contiguous subarray/substring.
* => with largest/smallest/average length/sum and k=window size
* => You must compute something for MANY overlapping ranges efficiently.
*
*
* Common statement patterns:
* 1) Fixed-size window (k given):
*    - "Maximum/Minimum sum of any subarray of size k"
*    - "Average of subarrays of size k"
*    - "Count of subarrays of size k satisfying condition"
*
* 2) Variable-size window (k not fixed; condition based):
*    - "Smallest/Longest subarray/substring with sum >= X"
*    - "Longest substring with at most K distinct characters"
*    - "Longest substring without repeating characters"

*
* Not Sliding Window Problem:
* - If order/contiguity doesn’t matter (subsequence, subset) -> usually different techniques.
* - If you must check all pairs/triples without a monotonic window condition -> may not fit.

* */
public class Introduction_1 {

}
